## 1. 알고리즘이란
- 세 값의 최댓값
```shell script
class Median
    static int med3(int a, int b, int c){
        if (a >= b)
            else if (b> = c)
                return b;
            else if (c >= a)
                return a;
            else 
                return c;
        else if (a > c)
          return a;
        else if (b > c)
          return c;
        else
          return b;
```